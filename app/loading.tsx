import TiffanyLoadingScreen from "@/components/shared/TiffanyLoadingScreen";

export default function Loading() {
  return (
    <TiffanyLoadingScreen
      message="Loading"
      detail="Please wait while Tiffany Cards prepares the page."
    />
  );
}